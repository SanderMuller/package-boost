composer.lock
/vendor
/node_modules
.cache
/build
/.idea
/.fleet
/.vscode
phpunit.xml
phpstan.neon
/coverage
RELEASE_NOTES*.md
.claude/scheduled_tasks.lock
/storage/logs
/tests/tmp

# Generated by `package-boost:sync` — regenerate locally, never committed.
# The test suite exercises the same filesystem paths and would clobber any
# committed copies on every `vendor/bin/pest` run.
/CLAUDE.md
/AGENTS.md
/.github/copilot-instructions.md
/.claude/skills
/.github/skills
